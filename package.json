{
  "name": "@yorz/cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "YorZ CLI — install/uninstall the yorz-spec skill into Claude Code / OpenCode.",
  "bin": {
    "yorz": "./dist/cli/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "test": "vitest run",
    "format": "prettier --write \"src/**/*.{ts,md}\" \"tests/**/*.ts\""
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
