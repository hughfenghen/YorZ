# TODO

- 项目列表、spec 列表 动态更新

## P1

- [ ] Skill 优化
  - [ ] 方案的影响范围、变更范围；spec review 都可能用到（差异是预估和实际影响）
  - [ ] Skill 的可测试性、效果评估
  - [ ] 可选深度、普通、简化版工作流，
    - [ ] 处理简单小问题、多次实施失败转入深度模式
- [ ] review 模式
  - [ ] 图形化 diff 变更表达
- [ ] UI / 功能 优化
  - [ ] 管理项目开发服务
    - [ ] 分享服务日志给 Agent
  - [ ] spec 列表，按天分组，降序
    - [ ] 按分类、模块过滤，名称、内容模糊匹配
  - [ ] 界面结构、布局、元素的美化
  - [ ] 快捷键系统
- [ ] 外部能力集成
  - [ ] 代码知识库、
    - [ ] 项目模块分析
    - [ ] 模块依赖关系
    - [ ] 模块与源码文件关系

- UI 美化
  - [ ] UI 主题色、美观度
  - [ ] 优化 Agent 输出效果，终端渲染库

## Bug

- [ ] 图形化区域显示纯代码文本，刷新后正常，可能是解析时机存在 bug
- [ ] 刷新登录态
- [ ] chat、spec 中的文件链接无法跳转

---

## Debug 模式

有时 Agent 执行任务时会陷入误区，多轮尝试都无法解决问题，甚至会越纠越偏或产生幻觉，认为自己已解决问题。

我需要为产品设计 Debug 模式功能，以下是我的想法，你可以发表意见建议，与我进行讨论；

期望功能入口在 @src/gui/src/pages/NewSpec.tsx @src/gui/src/pages/SpecDetail.tsx（追加任务）中选择 fix 时，新增一个 checkbox 选项：Debug 模式 （提示语：深度分析调试，尝试解决疑难问题）
用户勾选时，进入 Debug 模式，搭配 skill 指导 Agent 以资深工程师的 Debug 思路来定位并解决问题；

skill 流程与主要内容：

分析 bug 的相关信息 -> 规划 debug 方案，然后进入 假设 -> 实施 -> 验证 -> 缩小范围 -> 再假设 循环，最终确定根因
常见的方法有二分法、控制变量法、遍历可能分支等。

- 根据 Bug 描述，分析出关联模块与数据传递、函数调用链路，规划 Debug 步骤，可通过多个步骤逐渐缩小范围，确定最终根因
- 按规划步骤逐步推进，大胆假设、小心验证，真相必须有日志或图片等硬性证据支撑
- 可添加日志进行问题分析，要求用户回传或自行获取日志，进行验证
  - 确保日志内容可读、可复制，避免打印 object 数据
  - 避免循环打印大量日志

在 debug 模式，给与 Agent 更大的自由度

- 允许编写临时代码（如条件短路）或注释有意义代码，来控制程序进入或屏蔽特定逻辑分支
- 允许临时篡改代码变量值、Mock API 返回数据，让程序执行到特定逻辑分支
- 允许编写临时的独立测试页面或入口，来简化重现操作路径、直接验证怀疑环节（模块、逻辑分支）
- 允许联网查询第三方 SDK 文档，允许询问用户获取 Debug 必要信息
- 允许 lint typecheck build 单测出现错误，允许跳过这些检查

确定根因后并修复后，退出 debug 模式，收尾工作：

- 须还原临时代码、注释代码，确保原有逻辑的正确性，
- 须清理不需要的 debug 日志
- 进行常规的项目完整性检查，如：执行变更文件关联的单元测试、构建、类型检查
